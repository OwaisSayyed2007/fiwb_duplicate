[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["python312", "postgresql", "rustc", "cargo"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
